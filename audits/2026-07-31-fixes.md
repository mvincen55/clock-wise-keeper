# Fix prompt — 2026-07-31 audit (clock-wise-keeper / Purple Envelope)

You are a builder agent working on the repo `mvincen55/clock-wise-keeper` (Lovable + Supabase; branch `main`). This file is self-contained: everything you need is restated here. **Run ONE level at a time.** Levels are independent and ordered; a level may assume earlier levels are done but never requires a later one.

PRODUCT CONTEXT (read first): Multi-tenant practice-ops app for dental offices. React 18 + Vite + TS + shadcn/ui in `src/`; Supabase in `supabase/` (migrations are the schema documentation; edge functions on Deno; `supabase/config.toml` declares each function's `verify_jwt`). Roles: owner/manager/employee ("admins" = owner+manager). Golden rules (README): **"No patient data. Ever."** · **"org_id on every table, RLS on every table — RLS is the sole security perimeter"** · **"Org identity comes from org_members server-side, never from client input"** · **"Everything is a setting."** RLS helpers (SECURITY DEFINER): `is_org_member(org_id)`, `is_org_admin(org_id)`, `is_allowed_user()`. Dates are Eastern-local via `src/lib/time-utils.ts` (`getToday()`); edge functions use `easternToday()` (see `supabase/functions/checklist-bypass/index.ts:29-37`). Email: log `email_send_log` (status pending) BEFORE `enqueue_email` RPC; mask addresses in logs.

OPERATIONAL WARNING (from the specs): Lovable commits from stale snapshots and has reverted pages before. Work FORWARD from current code; never restore old page versions. After any level that adds/changes an edge function or migration, verify live per `docs/runbook.md` §1.

Each fix gives: WHAT/WHERE, WHY (quoted rule), ACCEPTANCE TEST (run it for real), ROLLBACK. Migrations: new timestamped file in `supabase/migrations/`; never edit an applied migration.

---

# LEVEL 1 — P0: rule violations, leaks, server-side enforcement gaps

**Accomplishes:** closes two server-side authorization holes (checklists, goals), three patient-data-to-AI paths, one exposed bank number, one unauthenticated cross-org function, and restores a regressed feature.
**Prerequisites:** none.
**Rough effort:** 1 focused day (fixes are small; verification is the work).
**Independently shippable:** yes — every fix here stands alone; ship immediately after the verification block passes.

### 1.1 Restore manager-audience protection on checklists (P0-01)

- **WHAT/WHERE:** New migration. The 2026-07-30 migration `20260730102959_711fb859-….sql:19-27` recreated `checklists_select` / `checklist_items_select` as `is_org_member(org_id) AND (owner_user_id IS NULL OR owner_user_id = auth.uid())`, dropping the audience rule. Rewrite both policies to keep the personal-list logic AND restore audience: a member may select a checklist only if `audience = 'all'` OR `is_org_admin(org_id)` OR it is their own personal list (`owner_user_id = auth.uid()`); items policies must join the parent checklist and apply the same rule. The correct pre-regression shape is in `supabase/migrations/20260723200000_checklists.sql:57-60,79-89` — merge it with the owner_user_id clauses, don't just revert (personal lists/capture depend on the new clauses).
- **WHY:** "on checklists the member can see (audience='all', plus 'manager' lists if the member is an owner/manager)" — `docs/goals-and-bypass-spec.md` §Prompt 2. The client filters audience in `src/hooks/useChecklistGating.ts:39-44` — UI-only enforcement is a P0 by definition.
- **ACCEPTANCE TEST:** With the public anon key as a non-admin employee: `GET /rest/v1/checklists?audience=eq.manager` → `[]`; same as a manager → rows. As employee: select items of a manager-audience list by id → `[]`. As employee: own personal list (owner_user_id = self) still readable and insertable.
- **ROLLBACK:** re-apply the 20260730102959 policy definitions (data is unaffected; policies are reversible).

### 1.2 Lock goals writes to the goal's owner; remove hard-delete (P0-02)

- **WHAT/WHERE:** New migration editing policies created in `supabase/migrations/20260730055651_d902d21b-….sql:93-97`. (a) goals UPDATE: replace `user_id = auth.uid() OR is_org_admin(org_id)` with `user_id = auth.uid() AND is_org_member(org_id)`. (b) DROP the DELETE policy entirely and `REVOKE DELETE ON public.goals FROM authenticated` — nobody hard-deletes; archival is a status update (columns `archived_at`/`archived_reason` exist in live types; Level 2 builds the UI, but the archive path is just an UPDATE, so this fix does not depend on Level 2). (c) Add the same current-`is_org_member` check to the UPDATE/DELETE policies of `work_style_profiles` (:118-123) and goal child tables where missing.
- **WHY:** "Only the goal's owner can edit or delete it — not managers, not owners of the org." / "Never hard-delete: set status='archived', archived_at, archived_reason" — `docs/goals-and-bypass-spec.md` §Prompt 7.
- **ACCEPTANCE TEST:** As an org owner via API (not UI): PATCH another member's goal title → 0 rows / permission error. DELETE another member's goal → denied. As the goal's owner: UPDATE status to 'archived' with a reason → succeeds. Confirm `goal_tasks`/`goal_updates`/`goal_messages` of that goal still exist afterward (no cascade).
- **ROLLBACK:** re-apply the 20260730055651 UPDATE/DELETE policy definitions.

### 1.3 Stop uploading verification documents verbatim to the AI gateway (P0-03)

- **WHAT/WHERE:** `supabase/functions/sprint-verify/index.ts:41-73`, `src/components/SprintVerifyDialog.tsx:90-104`, `src/hooks/useTeamGoals.ts:127-141`. (a) Add a persistent inline warning in SprintVerifyDialog at the upload control: "Verified against an external AI service — upload a totals-only export. Never include patient names or details." (same pattern as `src/pages/Assistant.tsx:196-198`). (b) In sprint-verify: after the verdict is rendered, delete the storage object and clear `verification_doc_path` (keep only the extracted metric + verdict + where-found reference) — the document must not persist. (c) Instruct the model in the prompt to extract ONLY the requested metric and to refuse/ask for a clearer export if the document contains person-level rows (mirrors the spec's brittleness rule). (d) If bytes must reach the model at all, add a server-side pass that strips anything that looks like a name column before sending, or restrict accepted inputs to CSV/PDF totals pages; if that is not feasible this pass, gate the feature behind an org setting default OFF and say so in your report.
- **WHY:** "No patient data. Ever. Not in tables, not in checklist titles, not in AI prompts." — README; "NO patient data anywhere" — `docs/intelligence-spec.md` §Prompt 16; "if the AI can't find the metric, it must say so and ask for a clearer export rather than guess" — intelligence-spec known risks.
- **ACCEPTANCE TEST:** Upload a test PDF containing a fake patient name → the warning is visible at the upload control before selecting a file. Complete a verification → the object is gone from the `sprint-verification` bucket and `team_goals.verification_doc_path` is null while the verdict row remains. Function logs show no document text persisted beyond the metric.
- **ROLLBACK:** re-enable previous sprint-verify revision; uncleared files remain in the bucket (harmless).

### 1.4 De-identify or gate every new free-text-to-AI path (P0-04)

- **WHAT/WHERE:** (a) `supabase/functions/goal-assistant/index.ts` — apply the same input guard used by training-builder (`guardAiInput`, see `supabase/functions/_shared/`) to `chat` mode messages (:355) and to `draft_update` inputs. In `draft_update` (:433,469,472): stop sending raw `checklist_items.title` — send counts and task titles from goal_tasks (structured, goal-owned) instead, or pass checklist titles through a scrub that rejects person-name patterns; keep quick notes but guarded. (b) `supabase/functions/office-pulse/index.ts:279,288,327-339` — same guard on goal/sprint titles before they enter brief prompts. (c) Add the persistent inline reminder ("Answers come from an external AI service — never include a patient's name or details", pattern at `src/pages/Assistant.tsx:196-198`) to: `src/components/goals/PathfinderChat.tsx` (under the input, always visible), `SetGoalCard.tsx` (under title/description), `GoalUpdateModal.tsx` (under quick notes), `src/components/training/BuildModuleDialog.tsx` (under topic). Model after `IncidentReportModal.tsx:172-173` which already does this right.
- **WHY:** "HIPAA tripwire: staff free text must never reach the AI gateway." — `docs/runbook.md` §10; REVIEW.md open item 2 names the UI reminder the accepted mitigation and a server-side scrub the real fix. Checklist titles are "business data only — never patient info" **by convention only** (migration 20260723200000 header) — convention is not enforcement.
- **ACCEPTANCE TEST:** Type "Call Jane Doe re crown" into Pathfinder chat → the guard strips/refuses before the gateway call (verify in function logs: the string never appears in the outbound payload). Trigger draft_update with a checklist item titled with a fake patient name → payload contains no such title (log inspection). The reminder line is visible while typing on all four surfaces (not only in an empty state).
- **ROLLBACK:** revert the payload-builder changes per mode; warnings are additive and need no rollback.

### 1.5 Column-restrict training_assignments assignee updates (P0-05)

- **WHAT/WHERE:** New migration adding a guard trigger on `training_assignments` modeled exactly on the checklist_bypasses guard in `supabase/migrations/20260730062218_….sql:35-56`: for a non-admin `authenticated` caller, only `status` and `completed_at` may change; any other column change raises an exception. (Pattern precedent: time_entries/tardies guard triggers from migration 20260720144829.)
- **WHY:** "RLS: admins create and read all in their org; the assignee reads their own and updates only their own status." — `docs/training-library-spec.md`. Today only the UI limits writes — UI-only enforcement is P0 by definition.
- **ACCEPTANCE TEST:** As an assignee via API: PATCH own assignment `due_date` → error/0 rows; PATCH `module_id` → error; PATCH `status='completed'` → succeeds. As admin: full update still works (trigger exempts admin/service paths like the existing guards do).
- **ROLLBACK:** drop the trigger.

### 1.6 Remove the bank account literal from the test suite (P0-06)

- **WHAT/WHERE:** `src/test/print-invariant.test.tsx:227` and ~:237, plus the pinned snapshot `src/test/__snapshots__/print-invariant.test.tsx.snap`. Replace the fixture `'Bay Coast Account #841845805'` with a placeholder (e.g. `'Test Bank Account #000000000'`), regenerate snapshots (`npx vitest run -u`), and grep the whole tree for `841845805` and `Bay Coast` — expected remaining hits: historical migrations and `docs/` only. While here, swap the other live-identity fixtures in the same files ("Harelick Dental Associates, LLC", "(508) 993-0515", "drharelick.com", "Megan Vincent") for neutral fixtures (inventory item 1.16).
- **WHY:** "Bank account number (2.2): treat as exposed. Remove the literal… no other literal account/routing numbers exist in the tree." — `docs/genericization/phase0-inventory.md`, Review decision #1 (2026-07-24).
- **ACCEPTANCE TEST:** `grep -rn "841845805\|Bay Coast" src/ supabase/functions/` → zero hits. `npx vitest run` → print-invariant suite passes on placeholder fixtures.
- **ROLLBACK:** none needed (test-only); keep the old snapshot in git history — history is not rewritten per the decision.

### 1.7 Authenticate office-pulse and register all functions in config.toml (P0-07)

- **WHAT/WHERE:** (a) `supabase/functions/office-pulse/index.ts:588-621` — add a service-role bearer gate using the constant-time key comparison pattern from `supabase/functions/process-email-queue/index.ts` (post-F6), exactly as `training-reminders/index.ts:99-105` already does. The function loops all orgs with the service key; it must answer 401 to anything else. (b) `supabase/config.toml` — add `[functions.<name>]` entries for every deployed function missing one: `office-pulse`, `training-reminders`, `accountability-engine`, `sprint-verify`, `support-agent`, and any others you find deployed (list `supabase/functions/` and diff against config.toml). `verify_jwt = true` for user-facing functions (sprint-verify, support-agent); scheduled service functions (office-pulse, training-reminders, accountability-engine) keep `verify_jwt = true` with the in-code service-role gate.
- **WHY:** "New edge function → [functions.<name>] entry in supabase/config.toml" — runbook change-checklist; "each does its own verification (webhook signature, service-role bearer…)" — README edge-functions section.
- **ACCEPTANCE TEST:** `curl -X POST $SUPA/functions/v1/office-pulse -H "apikey: $ANON" -d '{}'` → 401 (not 200). Same probe with a user JWT → 401. Every function dir has a config.toml entry; `grep -c "^\[functions\." supabase/config.toml` equals the deployed function count.
- **ROLLBACK:** remove the gate/commit — but treat the unauthenticated state as an incident, not a baseline.

### 1.8 Restore or formally retire training-roleplay (P0-08 — REGRESSED)

- **WHAT/WHERE:** First do a human-check step (5 min): probe `POST $SUPA/functions/v1/training-roleplay` with the anon key per runbook §1. If 401/4xx → it is deployed and only the repo lost it: recover the function source from Lovable history or the last known-good commit and re-commit it plus its `[functions.training-roleplay]` config entry and the ModulePlayer roleplay UI. If 404 → it is truly gone: rebuild per `docs/goals-and-bypass-spec.md` §Prompt 9 §4 (persona chat on the fast model, rubric scoring on the strong model at an 80% bar, `training_attempts.type` ('quiz'|'roleplay'), transcripts in answers jsonb under the same admin-privacy rule as quizzes — admins see pass/fail only). Whichever path: the roleplay chat is a member-free-text-to-AI surface — ship it WITH the guard + inline warning from fix 1.4, and keep "passing either assessment completes the assignment" intact.
- **WHY:** "training-roleplay … conversational assessment … Record in training_attempts with a new type field ('quiz' | 'roleplay') … Passing either marks the assignment complete." — goals-and-bypass-spec §Prompt 9 §4; the 2026-07-30 audit verified it existed — a regression means the fix didn't hold, so verify by probe, not by repo alone.
- **ACCEPTANCE TEST:** Probe returns 401 with no JWT (alive, gated). In UI: a module with a roleplay assessment offers "Practice the conversation"; completing a roleplay with a passing score marks the assignment completed; an admin views the assignment and sees status + pass/fail only (no transcript).
- **ROLLBACK:** if the rebuild destabilizes Training, hide the roleplay affordance behind a flag and file the function deploy as the only follow-up.

**LEVEL 1 VERIFICATION BLOCK — run and report all of it before claiming the level:**
1. `npx vitest run` green (including regenerated print-invariant snapshots).
2. The four API probes in 1.1/1.2/1.5 executed against the live project with a non-admin JWT (use `scripts/verify-rls.ts` as the harness pattern) — paste results.
3. `grep` evidence for 1.6.
4. Function-log evidence for 1.4 (payload inspection) and 1.3 (bucket purge).
5. `npx tsc -p tsconfig.app.json --noEmit` clean.
6. Deploy step (do LAST, after everything above passes): deploy every edge function changed in this level — `sprint-verify`, `goal-assistant`, `office-pulse`, `training-roleplay` (if rebuilt) — via Lovable prompt or `supabase functions deploy <name> --project-ref lfiplzmxpmybtbzhmnkp`, then probe each per runbook §1 and paste the HTTP codes. Migrations must be applied via Lovable or the dashboard — a GitHub push alone does not apply them.
7. Report anything you skipped and anything you did that wasn't asked for.

---

# LEVEL 2 — P1: spec requirements missing or wrong

**Accomplishes:** lands the two-goal-spec gaps that have survived two audits (S+M gate, edit/archive accountability), fixes the pre-refactor RLS crack, un-breaks three integrations (training-builder save, goal→checklist, bypass re-verification), and takes owners out of the time-clock/bypass flow.
**Prerequisites:** Level 1 (1.2's policy shape and 1.5's trigger pattern are referenced; 2.6 assumes 1.1's merged checklist policies).
**Rough effort:** 2–3 days. 2.2 is the big one (a full feature); the rest are hours each.
**Independently shippable:** yes, but 2.1+2.2 are entangled — do them in one sitting (the gate must apply to creation AND edits; 2.2 adds the edit flow).

### 2.1 S+M hard gate on goal save (P1-09 — STILL OPEN, 2 runs)

- **WHAT/WHERE:** `src/components/goals/SetGoalCard.tsx` (:139, :167, :46). Save stays disabled until: non-empty polished title AND non-empty `smart_target`; when polish returns SMART flags, `specific` and `measurable` must be true. Wire the gate to the existing SmartChips state with a plain hint by the failing chip ("make it measurable — add a number or count"). No red errors, no way to save without both. Achievable/Relevant/Time-bound chips stay advisory, never blocking. Expose the gate as a small pure function (e.g. `src/lib/goal-gate.ts`) so 2.2's edit flow reuses it — and so it is unit-testable (see Level 4, 4.3).
- **WHY:** "Save stays disabled until the goal is specific AND has a measurable target — concretely: non-empty polished title AND non-empty smart_target… Applies to creation and edits." — goals-and-bypass-spec §Prompt 10 item 2 (supersedes Prompt 8 line 5 and Prompt 9 §1).
- **ACCEPTANCE TEST:** Create goal with title only → Save disabled, hint visible by the Measurable chip. Add "10" as target → Save enables. With SMART flags where measurable=false → disabled regardless of target text. Unit test the pure gate: title+target → pass; missing either → fail; A/R/T flags never affect the result.
- **ROLLBACK:** revert SetGoalCard; the pure lib is additive.

### 2.2 Edit/archive goals with accountability — Prompt 7 verbatim (P1-10 — STILL OPEN, 2 runs)

- **WHAT/WHERE:** Implement `docs/goals-and-bypass-spec.md` §Prompt 7 exactly: (a) "Edit goal" on MyGoalCard → title + description; if the goal has ANY goal_updates, require a reason; never-shared goals edit freely. The S+M gate from 2.1 applies to edits. (b) "Delete goal" → modal requiring a reason before confirm enables; never hard-delete: `status='archived'`, `archived_at`, `archived_reason`; then immediately open the create-goal flow. (c) `goal_events` (id, org_id, goal_id, actor_id, type 'edited'|'archived'|'replaced', reason, old_title, new_title nullable, created_at) — the table exists in live types but has NO migration: create the migration WITH org_id + RLS matching the parent goal's visibility (team goal events → whole org reads; private → member + owners/managers), mirroring `can_view_goal`. (d) Meeting view: "Changes since last meeting" section ("Megan archived '…' — reason: … → New goal: '…'"); record 'replaced' with new_title once the successor exists; events also appear as subtle entries in the goal's update timeline. (e) goal-assistant `draft_update`: if the goal was edited/archived since the last update, the draft mentions the change and reason.
- **WHY:** "Let members edit and delete their own goals — with accountability, never silently." / "Goals are mutable, never silently" — goals-and-bypass-spec §Prompt 7 + product decision 12.
- **ACCEPTANCE TEST:** Edit a shared goal without a reason → save blocked; with a reason → a goal_events row exists with old/new title. Archive without reason → confirm disabled. Archive with reason → row archived (not deleted), card disappears, create flow opens, event recorded. Meeting view shows the change line. As another member via API: read goal_events of a private goal you don't own → 0 rows. draft_update on an edited goal → draft text references the change.
- **ROLLBACK:** feature-flag the edit/archive affordances; goal_events rows are append-only and harmless.

### 2.3 Fix the allowed_users RLS recursion (P1-11 — STILL OPEN, pre-refactor blocker)

- **WHAT/WHERE:** New migration replacing the self-referencing policy in `supabase/migrations/20260217234928_….sql:12-17` with one calling a SECURITY DEFINER helper (same pattern as `is_org_member`), per runbook §6's fix direction.
- **WHY:** "First refinement item, BEFORE refactor: fix the allowed_users RLS recursion (runbook §6) — don't build on the crack." — goals-and-bypass-spec header (2026-07-30).
- **ACCEPTANCE TEST:** As an authenticated non-service user: `SELECT email FROM allowed_users` → returns rows (no 500/42P17). `is_allowed_user()` behavior unchanged (sign-in gate still bounces non-allowlisted users — test with a non-allowlisted account in incognito).
- **ROLLBACK:** re-apply the old policy (restores the latent 500 but no data change).

### 2.4 Make training-builder's insert match the table (P1-12)

- **WHAT/WHERE:** Decide and align — the function and UI already expect `learning_style` and `audit` columns and a 'draft' status (ModuleAuditPanel, "Awaiting review" tab), so the cheaper coherent fix is a migration adding `learning_style` and `audit` (jsonb) to `training_modules` and extending the status CHECK to `('published','archived','draft')` — plus RLS so 'draft' modules are visible to admins only (the Awaiting-review flow depends on members NOT seeing unreviewed drafts). Alternative (if you judge the audit-panel shape wrong): strip :430-431 and map 'draft'→'published' in `supabase/functions/training-builder/index.ts:422-435`. Pick one, do it fully, say which in your report.
- **WHY:** "Save the result to training_modules (source 'pathfinder', status 'published') and return it." — training-library-spec; the whole goal-resource flow (goal-assistant/index.ts:296-335) dies here today.
- **ACCEPTANCE TEST:** Build a module from /training "Build with AI" → saved and visible per its status (no "could not be saved" toast). Trigger a Pathfinder breakdown that attaches a learning resource → module created with origin_goal_id set and linked on the goal card. As a non-admin: draft modules invisible.
- **ROLLBACK:** migration is additive + CHECK-widening (safe); the code path is reversible.

### 2.5 Fix "Add to my checklist" for non-admin members (P1-13)

- **WHAT/WHERE:** `src/hooks/useGoals.ts:286-308` inserts the 'My Goal Steps' list/item with `owner_user_id` unset, which the post-07-30 policies (20260730102959:30-37) reject for non-admins. Set `owner_user_id = auth.uid()` on both inserts (they ARE personal items — the gating hook already scopes owner_user_id items to their owner). Verify the WITH CHECK clauses accept it after Level 1's 1.1 merge.
- **WHY:** "Each accepted task gets an 'Add to my checklist' toggle that creates an item in the existing checklist system for that member on that due date" — goals-and-bypass-spec §Prompt 1 item 3.
- **ACCEPTANCE TEST:** As an employee (not admin): accept a plan, toggle "Add to my checklist" on a task → checklist item exists with owner_user_id = me, due today-per-task-date; it appears in my checklist and — if per-person daily — joins the clock-out gate. As another employee: cannot see my personal list (1.1's policy).
- **ROLLBACK:** revert the two inserts' field.

### 2.6 Align checklist-bypass server re-verification with the client gating set (P1-14)

- **WHAT/WHERE:** `supabase/functions/checklist-bypass/index.ts:168-190` must compute the same gating set as `src/hooks/useChecklistGating.ts:48-56,65-73`: daily + is_active + per_person + audience-appropriate lists, PLUS the `owner_user_id` (personal items count only for their owner) and `due_date` scoping the client applies, and completions matched on the same period_key semantics. Also: check the insert result at :203-225 — any error other than the idempotent conflict must return an error (never silently `{recorded: true}`). Extract the shared rule into a tested pure function if feasible (Level 4, 4.3 consumes it).
- **WHY:** "Re-verify the member's incomplete gating items for today — never trust the client count." — goals-and-bypass-spec §Prompt 2. A server that counts differently IS trusting nobody — it's just wrong independently.
- **ACCEPTANCE TEST:** Seed: member A has a personal (owner_user_id=A) incomplete daily item; member B clocks out with everything of B's complete → function returns `{recorded:false}` and writes nothing (today it would record a false bypass). Force a non-conflict insert failure (e.g. temporarily revoked grant in staging) → function returns an error, and the client still writes the clock-out punch (spec: never trap anyone at the office).
- **ROLLBACK:** revert the function to the prior revision (accepting the known false-positive rate) — document which you deployed.

### 2.7 Take owners out of the clock/bypass flow; gate it behind a setting (P1-15)

- **WHAT/WHERE:** `src/pages/Dashboard.tsx:184-202`, `src/hooks/useChecklistGating.ts:23,36`, `src/components/AppLayout.tsx` (BypassReasonBanner), `src/hooks/useGuardedClockAction.ts`. Add org setting `owners_clock_in` (default off) in the existing settings pattern. When off (default): owner role sees NO clock card, is excluded from gating audiences (owners don't punch → must never be gated, nagged by BypassReasonBanner, or emailed as a bypassing member), and Timesheet mirrors it. When on: current behavior. Scope discipline: this fix is the hard rule only. The full role-shaped layouts + Practice Pulse signal rebuild are a larger design task — list them as follow-up in your report, do NOT half-build them here.
- **WHY:** "OWNER layout (NO clock card — owners don't punch; but add an org setting 'owners clock in', default off)" — dashboard-spec §Prompt 11 v3; audit standing rule: the Owner role is excluded from checklist assignment/gating everywhere.
- **ACCEPTANCE TEST:** As owner (setting off): dashboard renders no clock card; complete zero checklist items and attempt clock-out paths → no bypass dialog exists because no punch UI exists; no BypassReasonBanner on any page; no checklist_bypasses row can be created for the owner via the client. Flip the setting on → clock card appears. As employee: unchanged behavior throughout.
- **ROLLBACK:** setting default off IS the rollback story; remove the setting to restore pre-fix behavior.

**LEVEL 2 VERIFICATION BLOCK — run and report all of it:**
1. `npx vitest run` green — including new unit tests for the goal gate (2.1).
2. Live API probes for 2.2 (goal_events visibility as a non-owner) and 2.3 (allowed_users select as authenticated user) — paste results.
3. End-to-end in the preview: build-with-AI module save (2.4), employee add-to-checklist (2.5), false-bypass seed test (2.6), owner dashboard (2.7).
4. Deploy step (LAST): deploy changed edge functions — `training-builder`, `checklist-bypass`, `goal-assistant` — then probe per runbook §1 and paste HTTP codes. Apply migrations via Lovable/dashboard; a GitHub push alone does not apply them.
5. Report anything you skipped and anything you did that wasn't asked for.

---

# LEVEL 3 — P2: genericization + settings registry

**Accomplishes:** finishes the approved genericization tail (membership/doctor/PTO hardcodes), moves office policy prose out of AI prompts into settings, and fixes three Eastern-time defects in sprint code.
**Prerequisites:** none beyond Level 1 (1.6 touched the same test files — rebase if needed).
**Rough effort:** half a day to a day.
**Independently shippable:** yes.

### 3.1 Membership brand out of print and deposit surfaces (P2-01, P2-02)
- **WHAT/WHERE:** `src/components/fof/FofPrintSheet.tsx` ~:290 (patient copy still prints "Included with Illumitrac Membership" — read `fof_settings.membership_plan_name` like the office copy already should); `src/pages/DepositLog.tsx` ~:262,:305 and `src/components/DepositPrintSheet.tsx:171` — add label fields to `org_deposit_settings` (the Phase 1 table already exists with wording slots; follow its pattern) for the membership row label and "Outside Financing", seeded 'Membership'/'Outside Financing' generically (Harelick's live rows keep 'Illumitrac' as org DATA, not code).
- **WHY:** "4.2/4.4 … office vocabulary → Setting 🖨" — phase0-inventory; "Everything is a setting." — README.
- **ACCEPTANCE TEST:** With the setting changed to "Acme Plan", the patient FOF copy, deposit screen, and printed office copy all show "Acme Plan"; snapshot tests updated and green (regenerate with placeholder fixtures per 1.6 — do NOT re-pin live values).
- **ROLLBACK:** revert to literals.

### 3.2 Doctor list and PTO fallbacks to settings (P2-03, P2-04)
- **WHAT/WHERE:** `src/pages/FofBuilder.tsx` — replace the `FOF_DOCTORS` literal with the existing `fof_settings.doctor_names` Json column (empty → hide the dropdown, don't invent names). `src/pages/PTO.tsx:51,55,56` — replace the personal initial state ('2022-02-07', '2026-02-14', -1.63) with empty/zero defaults.
- **WHY:** "5.2 … org-scoped doctor list" — phase0-inventory; "Everything is a setting." — README.
- **ACCEPTANCE TEST:** Fresh org with no settings rows: no doctor dropdown preselected with Harelick names; PTO page shows blanks/zeros, never a real person's dates/balance.
- **ROLLBACK:** revert.

### 3.3 Policy prose out of AI prompts (P2-05)
- **WHAT/WHERE:** `supabase/functions/kimi-agent/index.ts` (and `fof-assistant` only if still deployed — see Level 4, 4.5): replace the hardcoded POLICY_SUMMARY numbers/vocabulary ("10% standard; Illumitrac seniors +5%", "$1,000", "plans like Altus", "always 'delivered'", never-covered codes) with a block generated from live org settings + `fof_ai_guidance` rows at request time (the guidance-loading mechanism already exists in these functions, lines ~70-92 per inventory 6.4). Keep product-behavior rules (timing-first names, banned-words list, etc.) in code per the inventory's split.
- **WHY:** "Money/policy facts → generate this block from the Phase 2 settings registry so the assistant explains live org values, never stale prose." — phase0-inventory 6.1.
- **ACCEPTANCE TEST:** Change a discount value in settings → the assistant quotes the new value (function log shows the generated block). No "Illumitrac"/"Altus"/"Dr." literals remain in the prompt strings (`grep`).
- **ROLLBACK:** redeploy the previous function revision.

### 3.4 "The doctor" → setting-shaped copy (P2-06)
- **WHAT/WHERE:** `src/hooks/useGuardedClockAction.ts:53,57` and `src/components/ChecklistBypassDialog.tsx:38` — read the recipient label from the org messaging settings pattern (`org_messaging_settings` exists in this feature area), default "the doctor" for single-doctor orgs (Harelick unchanged), configurable for others.
- **WHY:** "Everything is a setting." — README product rule 2.
- **ACCEPTANCE TEST:** Change the label → dialog and both toasts render it; default org copy unchanged.
- **ROLLBACK:** revert.

### 3.5 Print CSS brand literals (P2-07)
- **WHAT/WHERE:** `src/index.css` @media print `.dep-checks th/td` — replace `#53406e`/`#d5cfe0` with the CSS custom properties BrandPrintStyle already injects (`var(--dep-navy)` / tint var), so org_branding.brand_color rules the printed sheet too.
- **WHY:** "1.9 … brand color in org_branding, injected as CSS custom property at render" — phase0-inventory.
- **ACCEPTANCE TEST:** Print-invariant snapshots regenerate green; a non-purple org_branding row produces non-purple check-table rules in print preview.
- **ROLLBACK:** revert.

### 3.6 Eastern-time defects in sprint code (P2-08, P2-09, P2-10)
- **WHAT/WHERE:** (a) `src/hooks/useMomentum.ts` "Shared before the meeting" — replace the hardcoded `-05:00` with the DST-correct `easternWallToUtcIso` helper in `src/lib/time-utils.ts` (it exists for exactly this). (b) `supabase/functions/goal-assistant/index.ts` — replace `new Date().toISOString().slice(0,10)` with an `easternToday()` computation (copy the Intl/en-CA pattern from `checklist-bypass/index.ts:29-37` or extract it to `_shared`). (c) `src/pages/Dashboard.tsx` — build the 14-day window start from an Eastern date key, not `toISOString().split('T')[0]`.
- **WHY:** "Display formatting always converts to America/New_York. Never mix these two concepts." — time-utils CONVENTION; "Eastern-local dates via src/lib/time-utils (getToday())" — goals spec §Prompt 2.
- **ACCEPTANCE TEST:** Unit-test (a): a meeting at 09:00 ET on a July (EDT) date — an update at 09:30 ET does NOT earn the badge; at 08:30 does. For (b): freeze time to 23:30 UTC (19:30 ET) → next-meeting lookup uses the Eastern date. `npx vitest run` green.
- **ROLLBACK:** revert each site independently.

### 3.7 Wire org_practice_settings (P2-11)
- **WHAT/WHERE:** `src/hooks/usePracticeVitals.ts` / `src/components/PracticeVitalsCard.tsx` — read `monthly_collections_target_cents` from `org_practice_settings` and pace the collections gauge against it (fall back to last-month pacing only when unset); wire `collections_visibility` so employees never see collections numbers when the setting hides them ("the collections visibility setting is respected everywhere" — dashboard-spec).
- **WHY:** "Collections gauge: month-to-date computed live from the deposit log vs the monthly_collections_target setting" — dashboard-spec §Prompt 11 v3.
- **ACCEPTANCE TEST:** Set target $50,000 → gauge reads pace vs 50k. Unset → falls back. Visibility off → employee role sees no collections figures anywhere.
- **ROLLBACK:** revert; setting unread again (current behavior).

**LEVEL 3 VERIFICATION BLOCK:**
1. `npx vitest run` green incl. regenerated snapshots (placeholder fixtures only — never live values).
2. `grep -rn "Illumitrac\|Altus\|drharelick\|(508) 993" src/ supabase/functions/` → only approved org-data paths remain (settings rows seeded as data, inventory-documented history).
3. Live probe: ask the assistant about the prepay discount → answer reflects the settings value.
4. Deploy step (LAST): deploy `kimi-agent` and `goal-assistant`; probe per runbook §1.
5. Report anything you skipped and anything you did that wasn't asked for.

---

# LEVEL 4 — P3: cleanup, dead code, missing tests

**Accomplishes:** removes drift hazards (lockfiles, orphaned functions), pins the accountability rules with tests, dedupes sprint churn, and makes the migration mirror authoritative again.
**Prerequisites:** Levels 1–2 (4.3 tests the rules those levels land; 4.5 depends on 3.3).
**Rough effort:** a day, mostly 4.3 and 4.8.
**Independently shippable:** yes; low risk throughout.

### 4.1 One lockfile (P3-01)
- **WHAT/WHERE:** Keep `package-lock.json` (Lovable/npm is the shipping toolchain); delete `bun.lock` and `bun.lockb`. Verify `npm ci && npm run test && npx vite build` from a clean checkout.
- **WHY:** P3 — duplication; installs silently diverge by tool.
- **ACCEPTANCE TEST:** clean-clone install/build/test green with npm only.
- **ROLLBACK:** restore files from git.

### 4.2 training-reminders: register or delete (P3-02 — STILL OPEN, 2 runs)
- **WHAT/WHERE:** Human-check first: is there a live cron calling it (Supabase dashboard → cron; probe per runbook §1)? If yes → add `[functions.training-reminders] verify_jwt = true` to `supabase/config.toml` (it already has the correct in-code service-bearer check, :99-105). If no → delete the function dir and its doc references.
- **WHY:** "Register the training-reminders function in supabase/config.toml with the correct verify_jwt for its caller" — audit-2026-07-30 Prompt 14 item 4; runbook change-checklist.
- **ACCEPTANCE TEST:** config.toml entry present with a live probe 401, or the directory gone with zero dangling references (`grep`).
- **ROLLBACK:** restore from git.

### 4.3 Tests on rule-enforcing paths (P3-03)
- **WHAT/WHERE:** Extract and test: (a) the gating-set computation shared by `useChecklistGating` and `checklist-bypass` (post-2.6) — daily/active/per-person/audience/owner_user_id/due_date rules, period_key formats; (b) the goal gate pure lib from 2.1; (c) `can_view_goal` visibility matrix as API tests in the `scripts/verify-rls.ts` style (team goal → org reads; private → member+admins; non-owner writes fail).
- **WHY:** P3 — "missing tests on rule-enforcing paths"; the client/server gating drift (P1-14) is what happens without them.
- **ACCEPTANCE TEST:** `npx vitest run` covers the three; break one rule deliberately (e.g. drop the audience clause) → a test goes red.
- **ROLLBACK:** none (additive).

### 4.4 Dedupe ProgressRing and date helpers (P3-04)
- **WHAT/WHERE:** `src/components/SprintCard.tsx` → import `src/components/goals/ProgressRing.tsx` (or promote it to `src/components/ProgressRing.tsx` and update both). Consolidate `shiftDate`/`shiftAnchor`/`mondayOf` (`src/lib/momentum.ts`, `src/pages/DepositLog.tsx`, `src/hooks/useChecklists.ts`) into `src/lib/time-utils.ts` — keep the UTC-noon arithmetic, it's correct; just stop triplicating it.
- **WHY:** P3 — duplication from sprint churn; independent drift guaranteed otherwise.
- **ACCEPTANCE TEST:** build + tests green; visual spot-check of sprint card and goal card rings.
- **ROLLBACK:** revert per file.

### 4.5 Retire fof-assistant (P3-05)
- **WHAT/WHERE:** After 3.3 (its unique content has migrated into kimi-agent's settings-driven block): delete `supabase/functions/fof-assistant/`, its `config.toml` entry (:21-23), and doc references; fix the comment drift in `_shared/procedure-notes.ts` and `kimi-agent/index.ts:18`. Probe live first (runbook §1) — if anything still calls it, migrate the caller to kimi-agent instead.
- **WHY:** P3 — orphaned artifact; an unmaintained deployed AI endpoint.
- **ACCEPTANCE TEST:** zero repo references; live probe 404 after removal; /assistant works via kimi-agent.
- **ROLLBACK:** restore from git + redeploy.

### 4.6 Persistent no-PHI note in the FOF Assistant widget (P3-06)
- **WHAT/WHERE:** `src/components/fof/FofAssistantWidget.tsx:228-232` — move the "Never include patient names…" line under the input, persistent (pattern: `src/pages/Assistant.tsx:196-198`).
- **WHY:** the accepted mitigation for REVIEW open item 2 requires the reminder at the point of typing, not the empty state.
- **ACCEPTANCE TEST:** send a message → reminder still visible while typing the next one.
- **ROLLBACK:** revert.

### 4.7 TimeVault sweep on agent/MCP surfaces (P3-07)
- **WHAT/WHERE:** `src/lib/mcp/index.ts`, `supabase/functions/mcp/index.ts`, `.lovable/mcp/manifest.json`, `supabase/functions/kimi-agent/index.ts` (SITE prompt, X-Title, commit trailer), `supabase/functions/assistant-auditor/index.ts` (X-Title), `capacitor.config.json` appId, `src/components/WipeDataTool.tsx` backup filename → "Purple Envelope" naming. Leave localStorage keys and the repo name alone (README: coordinated relink required).
- **WHY:** "Rebrand incomplete… Sweep pending." — README known issue #2.
- **ACCEPTANCE TEST:** `grep -rni "timevault\|timekeeper" src/ supabase/functions/ capacitor.config.json` → only documented legacy (localStorage keys, ALLOWED_ORIGINS, history) remains.
- **ROLLBACK:** revert.

### 4.8 Make the migration mirror authoritative (P3-08)
- **WHAT/WHERE:** Reconcile live-only schema into migrations: `goal_events` (also required by 2.2 — coordinate, one migration serves both), `conversations`/`conversation_participants`/`messages` (+ `is_conv_participant`), `security_events`, `user_notes` (write the owner-only RLS the dashboard spec promises: "MY NOTES (user_notes, strictly owner-only RLS)"), `employees.team`. Source of truth: introspect the live DB (Supabase dashboard or `supabase db diff`) and write additive migrations that match live exactly. Fix the broken chain: `20260724130206` duplicates CREATE TABLEs from 20260723200000/20260723210000 with a divergent deposit policy — make it idempotent (`IF NOT EXISTS` + policy drop/create guards) or mark it superseded per your migration tooling; 20260730172109/20260730172500 must apply after the tables they index exist. End state: `supabase db reset` (or a fresh project apply) runs the full chain clean.
- **WHY:** "The repo's migration history is the schema documentation — read it before writing SQL." — README §Database conventions. Today it isn't, and two audits have hit objects they couldn't verify because of it.
- **ACCEPTANCE TEST:** fresh apply of the full migration chain succeeds; `supabase db diff` against live returns empty; the user_notes policy check: as another org member, select someone's user_notes → 0 rows.
- **ROLLBACK:** additive-only; the chain fix is ordering/idempotency, not data change.

**LEVEL 4 VERIFICATION BLOCK:**
1. Clean-clone `npm ci && npm run test && npx vite build` green.
2. Fresh migration-chain apply succeeds; diff vs live is empty.
3. `grep` evidence for 4.2/4.5/4.7.
4. New tests demonstrably red when a rule is broken (4.3).
5. Deploy step (LAST, only if 4.5 deleted or 4.2 registered functions): reconcile live deployments with the repo state; probe per runbook §1.
6. Report anything you skipped and anything you did that wasn't asked for.

---

## Appendix — suppressed findings (over the 15-finding cap), for scheduling

These were verified but suppressed by the audit's cap. Fix notes, brief:

1. **Practice Pulse rebuild** (P1-class): orb computes only deposit-vitals signals and renders admin-only — rebuild per dashboard-spec §Prompt 11 v3 §1 (all roles, very top; signals: missing shifts, unresolved bypasses, pending approvals, collections pace, shared-checklist completion + disruption spike per Prompt 15). Naturally pairs with the role-shaped dashboard work deferred from fix 2.7.
2. **.limit(1) org derivation** (P1-class): `checklist-bypass/index.ts:122-127`, `goal-assistant/index.ts:107-112`, `training-builder/index.ts:171-176` (and `ingest-doc/index.ts:68-79`, pre-existing) resolve "the user's org" nondeterministically for multi-org users. Accept an explicit org context and VALIDATE membership against it; single-org behavior unchanged. Needs a multi-org test account to verify.
3. **Goals Report footer pinning** (P1-class): `src/index.css` — give `.goal-sheet` `min-height: 10in; display:flex; flex-direction:column` (copy `.dep-sheet`) and `.goal-page-footer` `margin-top:auto`. Verify in print preview with a one-member report.
4. **Goals Report snapshot coverage** (P1-class): add `GoalsPrintSheet` to `src/test/print-invariant.test.tsx` (placeholder fixtures per fix 1.6). Would have caught #3.
5. **Training auditor as spec'd** (Prompt 12 — STILL OPEN, partial): a different audit shape landed (ModuleAuditPanel, Awaiting-review tab, audit-diff badge). Decide: adopt the shipped shape and update the spec, or build the spec'd fingerprinted `training-auditor` + `training_audit_findings`. Human decision needed before any agent work.

When a level is done, append a one-line status (date + commit) to this file so the next audit can classify FIXED vs STILL OPEN.
