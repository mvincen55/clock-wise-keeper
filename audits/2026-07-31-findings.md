# Audit findings — 2026-07-31

- **Run date:** 2026-07-31
- **Commit:** `b5de48d3e7cdb6baf6b7c7d8ee5b23f37bca2112` (main, "Added RLS suppression tests")
- **Spec files read:** `docs/goals-and-bypass-spec.md`, `docs/dashboard-spec.md`, `docs/training-library-spec.md`, `docs/messaging-spec.md`, `docs/intelligence-spec.md`, `docs/genericization/phase0-inventory.md` (reference: `README.md`, `docs/runbook.md`, `REVIEW.md`, `docs/team-onboarding.md` not needed this run)
- **Path note:** the audit protocol names `/docs/specs/` and `/audits/`. This repo keeps specs directly in `docs/` (filenames `*-spec.md`) and the prior report at `docs/audit-2026-07-30.md`. Audited against those; recommend the next agent create `audits/` (this run does) and optionally move specs under `docs/specs/` so the protocol paths are literal next time.
- **Counts:** P0 = 8 · P1 = 7 · P2 = 11 · P3 = 8 (caps respected: 15 P0+P1, 34 P2+P3 allowed 25 — we report 19 and suppress 0 P2/P3; **8 P1-class findings suppressed**, listed in the footer)
- **Diff vs last run (2026-07-30):** 12 NEW · 4 STILL OPEN · 2 REGRESSED · 3 FIXED/verified (see classification section)

---

## P0 — Level 1

### [P0-01] Manager-audience checklists readable by every member — RLS regression
Dimension: 5 — multi-tenancy
Location: supabase/migrations/20260730102959_711fb859-038f-45da-9e5c-61df06a2c2db.sql:19-27
Status: REGRESSED (audience check correctly implemented in 20260723200000_checklists.sql:57-60, dropped 2026-07-30)
Confidence: certain
Violates: "on checklists the member can see (audience='all', plus 'manager' lists if the member is an owner/manager)" — goals-and-bypass-spec §Prompt 2; checklist model README §Checklist data model
Problem: The 07-30 migration drops "Members read checklists"/"Members read checklist_items" and recreates them as `is_org_member(org_id) AND (owner_user_id IS NULL OR owner_user_id = auth.uid())` — the audience check is gone and the items policy no longer joins the parent list's audience. Any employee can SELECT manager-only checklists and items with the public anon key.
Blast radius: every checklist consumer — useChecklists, useChecklistGating (filters audience client-side only, src/hooks/useChecklistGating.ts:39-44), the checklist-bypass function; manager-only operational content exposed org-wide.

### [P0-02] Goals RLS lets org admins edit/delete any member's goal — and hard-delete exists at all
Dimension: 2 — rule enforcement
Location: supabase/migrations/20260730055651_d902d21b-1714-4797-b123-44db7a56e643.sql:93-97
Status: NEW
Confidence: certain
Violates: "Only the goal's owner can edit or delete it — not managers, not owners of the org." and "Never hard-delete: set status='archived'…" — goals-and-bypass-spec §Prompt 7
Problem: The UPDATE policy (`user_id = auth.uid() OR is_org_admin(org_id)`) and the DELETE policy let any owner/manager rewrite or hard-delete a member's goal via the API; only the UI hides it. The policies also omit a current-`is_org_member` check, so a removed member may retain write access to their own rows.
Blast radius: goal_tasks/goal_updates/goal_messages cascade on hard delete (`ON DELETE CASCADE`), destroying the accountability trail Prompt 7 exists to protect.

### [P0-03] Sprint document verification uploads the office's outside report verbatim to the non-BAA AI gateway
Dimension: 4 — PHI surface
Location: supabase/functions/sprint-verify/index.ts:41-73 (forward at :72); src/components/SprintVerifyDialog.tsx:90-104; src/hooks/useTeamGoals.ts:127-141
Status: NEW
Confidence: certain (code path) / likely (PHI exposure)
Violates: "No patient data. Ever. Not in tables, not in checklist titles, not in AI prompts." — README; "NO patient data anywhere" — intelligence-spec §Prompt 16
Problem: A manager uploads "the outside report the office uses as the source of truth" — in a dental practice typically a PMS production/recall/collections report, a document class that routinely contains patient names — and the raw bytes go base64'd to the AI gateway with no de-identification and no no-PHI warning anywhere in the dialog. The file then persists indefinitely in the `sprint-verification` bucket (`verification_doc_path` on team_goals, no purge), and the model's verdict can quote document text back into the DB.
Blast radius: storage bucket retention; `ai_verdict.where/reasoning` persisted on team_goals; the strongest HIPAA-boundary breach in the tree today.

### [P0-04] Member/staff free text reaches the non-BAA AI gateway through new sprint surfaces
Dimension: 4 — PHI surface
Location: supabase/functions/goal-assistant/index.ts:355,386-400 (chat: message + full thread verbatim); :433,469,472 (draft_update: checklist item titles, quick notes, recent goal_updates content); supabase/functions/office-pulse/index.ts:279,288,327-339 (goal/sprint titles in scheduled briefs); no warnings in src/components/goals/PathfinderChat.tsx, SetGoalCard.tsx, GoalUpdateModal.tsx
Status: NEW
Confidence: certain
Violates: "HIPAA tripwire: staff free text must never reach the AI gateway." — runbook §10; REVIEW.md open item 2 (accepted mitigation = persistent UI reminder; server scrub = the real fix)
Problem: Pathfinder chat sends the member's typed message and the entire thread verbatim with no scrub and — unlike Ask AI — no UI reminder at the point of typing. Server-side paths are worse: `draft_update` and office-pulse pull checklist item titles and goal text (free text protected by convention only) into AI payloads where no typing-point warning is even possible.
Blast radius: one checklist renamed "Call Jane Doe re crown" silently becomes an AI payload on the next update draft; goal_messages persisted and re-sent every turn; thread content reused by draft_update shown at team meetings.

### [P0-05] training_assignments assignee UPDATE is not column-restricted
Dimension: 2 — rule enforcement
Location: supabase/migrations/20260730063350_3005cac7-e1ee-48ab-81da-0449dc308797.sql:71-73
Status: NEW
Confidence: certain
Violates: "RLS: admins create and read all in their org; the assignee reads their own and updates only their own status." — training-library-spec §The prompt
Problem: "Assignee updates own assignment" restricts the row, not the columns — an assignee can UPDATE due_date, module_id, assigned_by, completed_at on their own row via API; only the UI limits writes to status. No guard trigger (contrast checklist_bypasses, which got one).
Blast radius: overdue/amber reporting and due-date accountability on My Training are member-editable.

### [P0-06] Real bank account number re-committed via the print-invariant tests
Dimension: 3 — hardcoded office specifics
Location: src/test/print-invariant.test.tsx:227 (`accountLine: 'Bay Coast Account #841845805'`), asserted ~:237; pinned in src/test/__snapshots__/print-invariant.test.tsx.snap
Status: NEW (REGRESSED-adjacent: the literal was removed from DepositPrintSheet per decision #1, then re-introduced in tests)
Confidence: certain (literal present); whether the test exemption was deliberate needs human check
Violates: "Bank account number (2.2): treat as exposed. Remove the literal in Phase 1… no other literal account/routing numbers exist in the tree." — phase0-inventory, Review decision #1 (2026-07-24)
Problem: The Phase 1 snapshot suite re-committed the same account number as a fixture and pinned snapshot; the invariant could pin a placeholder instead. The decision's premise no longer holds.
Blast radius: every snapshot regeneration re-derives from the fixture; any future repo-visibility flip re-exposes it; same files re-pin "Harelick"/phone/website fixtures (inventory 1.16, still NOT DONE).

### [P0-07] office-pulse: unauthenticated service-role function operating over ALL orgs (+ missing config.toml entries)
Dimension: 5/6 — multi-tenancy / integration
Location: supabase/functions/office-pulse/index.ts:588-621 (service-role client :592-596, loops `from("orgs")` :605, no caller check); supabase/config.toml (no [functions.office-pulse], [functions.training-reminders], [functions.accountability-engine], [functions.sprint-verify], [functions.support-agent] entries)
Status: NEW
Confidence: certain (code); live gateway default verify_jwt behavior needs human check
Violates: "New edge function → [functions.<name>] entry in supabase/config.toml" — runbook change-checklist; contrast training-reminders/index.ts:99-105 which does check the service-role bearer
Problem: office-pulse performs cross-org writes (reminder_hooks, office_nudges, team sprint updates, AI calls) with the service key and never authenticates the caller; if the gateway accepts the public anon key, anyone can trigger org-wide side effects and AI spend. Nine other functions also lack config entries.
Blast radius: every org's notifications/nudges/sprints; email + AI credit burn; deployment config drift.

### [P0-08] training-roleplay regressed — function and config entry gone from repo
Dimension: 1 — spec conformance
Location: absent: supabase/functions/training-roleplay/ and any [functions.training-roleplay] in supabase/config.toml; ModulePlayer.tsx has no roleplay UI
Status: REGRESSED (2026-07-30 audit verified "training-roleplay function exists + in config.toml"; escalation applied per re-run rule)
Confidence: needs human check (live deployment may still serve it; the repo at HEAD does not)
Violates: "training-roleplay … conversational assessment … Record in training_attempts with a new type field ('quiz' | 'roleplay')" — goals-and-bypass-spec §Prompt 9 §4
Problem: The function, config entry, and UI verified yesterday have disappeared — consistent with the spec's documented Lovable stale-snapshot revert pattern ("Lovable is committing from stale snapshots… older versions of /goals have overwritten newer ones").
Blast radius: Prompt 9 §4 unimplemented at HEAD; types.ts still carries roleplay shapes → repo/live drift; passing-either-assessment completion path broken for roleplay modules.

---

## P1 — Level 2

### [P1-09] S+M hard gate still not landed
Dimension: 1 — spec conformance
Location: src/components/goals/SetGoalCard.tsx:167 (Save enables on any non-empty title), :139 ("How you'll measure it (optional)"), :46 ("Polishing is a nicety — never block saving a goal")
Status: STILL OPEN (2 runs)
Confidence: certain
Violates: "Save stays disabled until the goal is specific AND has a measurable target — concretely: non-empty polished title AND non-empty smart_target, and when polish returns SMART flags, specific+measurable must be true" — goals-and-bypass-spec §Prompt 10 item 2; audit-2026-07-30 Prompt 14 item 1
Problem: Save enables on any non-empty title; smart_target is optional and SmartChips never gate. No edit flow exists where the gate could apply either.
Blast radius: every goal created in the org; Prompt 10 consolidation item 2.

### [P1-10] Prompt 7 (edit/archive goals with accountability) still not built
Dimension: 1 — spec conformance
Location: src/components/goals/MyGoalCard.tsx (no edit/delete affordance); src/pages/Goals.tsx (no "Changes since last meeting"); goal_events/archived_reason occur only in src/integrations/supabase/types.ts — zero usage in src/ or supabase/
Status: STILL OPEN (2 runs)
Confidence: certain
Violates: Prompt 7 verbatim — "Let members edit and delete their own goals — with accountability, never silently… New table goal_events… Meeting view gets a 'Changes since last meeting' section" — goals-and-bypass-spec §Prompt 7
Problem: No edit/archive UI, no event recording, no meeting surface. goal_events and goals.archived_at/archived_reason exist in live types but are orphaned — and no migration creates them (repo/live drift, see P3-08).
Blast radius: goal-assistant draft_update cannot mention changes; compounds P0-02 (admin delete path exists while the spec'd archive path doesn't).

### [P1-11] allowed_users RLS infinite recursion (42P17) still not fixed
Dimension: 5 — multi-tenancy
Location: supabase/migrations/20260217234928_221f76f1-5830-411d-b777-bf4a3afe6699.sql:12-17
Status: STILL OPEN (known since 2026-07; named the pre-refactor blocker in the goals spec header 2026-07-30)
Confidence: certain
Violates: "First refinement item, BEFORE refactor: fix the allowed_users RLS recursion (runbook §6) — don't build on the crack." — goals-and-bypass-spec header; runbook §6
Problem: Policy "Allowed users can read allowlist" still self-references `allowed_users`; no fix migration exists. Direct authenticated queries can 500; the app survives only via the SECURITY DEFINER is_allowed_user() bypass.
Blast radius: anything querying allowed_users outside the RPC; the entire planned refactor is told not to build on this.

### [P1-12] training-builder can never save a module — insert doesn't match the table
Dimension: 6 — integration surface
Location: supabase/functions/training-builder/index.ts:422-435 vs supabase/migrations/20260730063350_….sql:1-16 (status CHECK at :10)
Status: NEW
Confidence: certain
Violates: training spec flow "calls the training-builder function… Save the result to training_modules (source 'pathfinder', status 'published')" — training-library-spec; runbook change discipline
Problem: The insert includes columns `learning_style` (:430) and `audit` (:431) no migration adds, so PostgREST rejects every insert; the auditor-hold path also inserts `status='draft'` which violates `CHECK (status IN ('published','archived'))`. Every build — manual or Pathfinder-triggered — fails with "The module was written but could not be saved."
Blast radius: goal-assistant resource path (goal-assistant/index.ts:296-335), goal_tasks.training_module_id linking, the whole Training Library; AI credits burned on every attempt.

### [P1-13] "Add to my checklist" is rejected by RLS for non-admin members
Dimension: 6 — integration surface
Location: src/hooks/useGoals.ts:286-308 vs supabase/migrations/20260730102959_….sql:30-37 and 20260723200000_checklists.sql:63-67,92-96
Status: NEW
Confidence: likely (static policy analysis; one runtime confirm recommended)
Violates: "Each accepted task gets an 'Add to my checklist' toggle that creates an item in the existing checklist system for that member on that due date" — goals-and-bypass-spec §Prompt 1 item 3
Problem: The client inserts a shared 'My Goal Steps' list/item with `owner_user_id` unset; after the 07-30 policy rewrite, non-admin INSERT requires owner_user_id = auth.uid() and the only other write policy is is_org_admin. A regular employee's insert fails server-side.
Blast radius: goals→checklists integration, and therefore the clock-out gate (accepted plan tasks are meant to become gating commitments).

### [P1-14] checklist-bypass server re-verification computes a different gating set than the client (+ unchecked insert error)
Dimension: 1/2 — spec conformance / rule enforcement
Location: supabase/functions/checklist-bypass/index.ts:168-190 vs src/hooks/useChecklistGating.ts:48-56,65-73; insert error unchecked at :203-225
Status: NEW
Confidence: certain
Violates: "Re-verify the member's incomplete gating items for today — never trust the client count." — goals-and-bypass-spec §Prompt 2
Problem: The function ignores `checklist_items.owner_user_id` and `due_date` (added by migration 20260730102959) and only accepts completions with period_key = today, while the client scopes personal/captured items to their owner and day. The server count can be inflated by other people's personal items → bypass rows, escalation levels, and owner/manager emails when nothing was actually incomplete. Separately, a non-conflict insert failure silently returns `recorded: true` with no audit row or notifications.
Blast radius: false checklist_bypasses rows and escalation wording; trust in the whole accountability loop.

### [P1-15] Dashboard is not role-shaped — owners get a clock card, can punch, and get checklist-gated
Dimension: 1 — spec conformance
Location: src/pages/Dashboard.tsx:184-202 (clock card + Clock In/Out with no role check; only isManager gates OrgSnapshotPanel/PracticeVitalsCard at :134-136); src/hooks/useChecklistGating.ts:23,36 (owners/managers included in gating audiences)
Status: NEW (prior run noted "role-shaped work started"; at HEAD the role shapes are absent)
Confidence: certain
Violates: "OWNER layout (NO clock card — owners don't punch; but add an org setting 'owners clock in', default off)" and "Role-shaped, not role-filtered." — dashboard-spec §Prompt 11 v3; audit standing check: Owner excluded from checklist assignment/gating
Problem: One shared layout for all roles; an owner sees the clock, can punch, and on clock-out hits the bypass dialog/nag loop owners must never enter. No "owners clock in" setting exists; owner-spec widgets (collections gauge vs monthly_collections_target, payroll % of collections, staffing strip) are absent.
Blast radius: checklist-bypass inserts bypass rows + emails for owners; BypassReasonBanner nags them on every page.

---

## P2 — Level 3

### [P2-01] Patient-facing FOF prints "Included with Illumitrac Membership"
Dimension: 3/8 — Location: src/components/fof/FofPrintSheet.tsx ~:290 (patient copy; office copy ~:430 already generic)
Status: NEW · Confidence: certain
Violates: "4.4 … 'Included with Illumitrac Membership' printed line — Setting 🖨: derive from membership plan display name" — phase0-inventory (Phase 2c)
Problem: Harelick's membership brand is hardcoded on the patient-facing form although `fof_settings.membership_plan_name` exists; the print sheet takes no settings prop to read it.
Blast radius: every patient-facing FOF for any org.

### [P2-02] "Illumitrac" labels hardcoded on Deposit Log screen and print
Dimension: 3/8 — Location: src/pages/DepositLog.tsx ~:262,:305; src/components/DepositPrintSheet.tsx:171
Status: NEW · Confidence: certain
Violates: "4.2 … the row labels 'Illumitrac' / 'Outside Financing' are office vocabulary → Setting 🖨" — phase0-inventory
Problem: On-screen form and printed office copy name Harelick's membership program; org_deposit_settings has no field for these row labels.
Blast radius: deposit log UI + both printed copies.

### [P2-03] FOF_DOCTORS hardcoded; doctor_names setting column unread
Dimension: 3 — Location: src/pages/FofBuilder.tsx (`const FOF_DOCTORS = ['Dr. Scott','Dr. Jennie','Dr. Robert','Dr. Nicole','Dr. Natalie']`)
Status: NEW · Confidence: likely
Violates: "5.2 … FOF_DOCTORS … Setting 🖨: org-scoped doctor list" — phase0-inventory (Phase 2c)
Problem: `fof_settings.doctor_names` Json column exists but nothing reads it; a new office sees Harelick's five doctors.
Blast radius: FOF builder dropdown, printed doctor name.

### [P2-04] Employee-specific PTO values hardcoded as initial form state
Dimension: 3 — Location: src/pages/PTO.tsx:51,55,56 (hire date '2022-02-07', snapshot date '2026-02-14', balance -1.63)
Status: NEW · Confidence: certain (present); impact needs human check (DB settings overwrite on load for the live org)
Violates: "Everything is a setting." — README product rule 2
Problem: One employee's actual hire date and PTO balance ship as fallback state; a fresh org with no pto_settings row sees them.
Blast radius: first-run PTO page for any new org.

### [P2-05] Office policy prose embedded in AI assistant prompts (POLICY_SUMMARY)
Dimension: 3/8 — Location: supabase/functions/fof-assistant/index.ts (POLICY_SUMMARY: "10% standard; Illumitrac seniors +5%", "$1,000", "plans like Altus", "always 'delivered'", never-covered codes); duplicated in supabase/functions/kimi-agent/index.ts
Status: STILL OPEN (inventory 6.1, scheduled for Phase 2/3; confirmed present at HEAD)
Confidence: certain
Violates: "6.1 … Money/policy facts → generate this block from the Phase 2 settings registry so the assistant explains live org values, never stale prose" — phase0-inventory
Problem: The assistants recite Harelick's policy numbers and vocabulary to staff of any org; fof_ai_guidance exists but these strings haven't migrated.
Blast radius: every AI answer about discounts/coverage; contradicts live settings if an org changes them.

### [P2-06] New sprint copy hardcodes "the doctor"
Dimension: 8 — Location: src/hooks/useGuardedClockAction.ts:53,57; src/components/ChecklistBypassDialog.tsx:38
Status: NEW · Confidence: certain (strings); whether deliberate product voice needs human check
Violates: "Everything is a setting." — README product rule 2 (org_messaging_settings label pattern already exists in the same feature area)
Problem: Bypass toasts/dialog bake a one-doctor-owner org shape into user-facing copy.
Blast radius: every bypass dialog/toast/email wording echo.

### [P2-07] Print CSS re-states brand purple as literals
Dimension: 8 — Location: src/index.css @media print: `.dep-checks th { border-bottom: 1.25pt solid #53406e; }`, `.dep-checks td { … #d5cfe0; }`
Status: NEW · Confidence: certain
Violates: "1.9 … brand color in org_branding, injected as CSS custom property at render" — phase0-inventory
Problem: The sheet elsewhere uses var(--dep-navy) (org-overridable via BrandPrintStyle); these two print rules hardcode purple/tint, so a re-branded org still prints purple rules.
Blast radius: printed Deposit Log check table.

### [P2-08] "Shared before the meeting" badge hardcodes a -05:00 cutoff — wrong during EDT
Dimension: 9 — timezone regression class
Location: src/hooks/useMomentum.ts ("Shared before the meeting": `new Date(\`${m.event_date}T…-05:00\`)`)
Status: NEW · Confidence: certain (bug), impact low
Violates: time-utils CONVENTION — "Display formatting always converts to America/New_York. Never mix these two concepts"; REVIEW A2 (DST-correct ET↔UTC conversion)
Problem: For ~8 months/year (EDT, UTC-4) the meeting-start cutoff is computed one hour late; an update posted up to an hour after the meeting started still earns the badge.
Blast radius: one motivational badge; but it's a new hardcoded-offset date computation in sprint code — the class the 2026-07-07 review claimed eliminated.

### [P2-09] goal-assistant computes "today" as the UTC calendar date
Dimension: 9 — timezone regression class
Location: supabase/functions/goal-assistant/index.ts (`const todayIso = new Date().toISOString().slice(0,10)` — next-meeting lookup; identical in breakdown/planFrom)
Status: NEW · Confidence: certain
Violates: "Eastern-local dates via src/lib/time-utils (getToday())" — goals-and-bypass-spec §Prompt 2 (client analog; edge convention elsewhere is easternToday() per checklist-bypass/office-pulse)
Problem: Between ~7/8pm and midnight Eastern, UTC date is the next day: the "next team meeting" query can return an already-past meeting and plan scheduling starts a day late.
Blast radius: Pathfinder plan scheduling + meeting-awareness copy. Fail-soft but wrong daily.

### [P2-10] Dashboard 14-day missing-shifts window uses a fake-UTC date
Dimension: 9 — timezone regression class
Location: src/pages/Dashboard.tsx (`fourteenDaysAgo.setDate(…-14); useMissingShifts(fourteenDaysAgo.toISOString().split('T')[0])`)
Status: NEW · Confidence: needs human check (low impact: read window edge, not a writer)
Violates: Eastern-local date convention (getToday()/easternDateKey)
Problem: toISOString().split('T')[0] yields the UTC date — one day ahead of Eastern after ~7/8pm.
Blast radius: useMissingShifts window start edge only.

### [P2-11] org_practice_settings row created but unwired (collections target gauge)
Dimension: 8 — Location: src/hooks/usePracticeVitals.ts + src/components/PracticeVitalsCard.tsx (paced vs last month instead); org_practice_settings.monthly_collections_target_cents exists in types.ts with zero reads outside it
Status: NEW · Confidence: likely
Violates: "Collections gauge: month-to-date computed live from the deposit log vs the monthly_collections_target setting" — dashboard-spec §Prompt 11 v3
Problem: The settings row was created but never read; the configured target can never affect the dashboard. Same table's collections_visibility and roleplay_* columns also appear unread.
Blast radius: owner/manager vitals cards; Practice Pulse signals.

---

## P3 — Level 4

### [P3-01] Three lockfiles at repo root
Dimension: 7 — Location: /bun.lock, /bun.lockb, /package-lock.json
Status: NEW · Confidence: certain
Problem: bun text + bun binary + npm lockfiles coexist; installs can silently diverge by tool.
Blast radius: CI/contributor dependency drift.

### [P3-02] training-reminders orphaned — no config.toml entry, no caller
Dimension: 7 — Location: supabase/functions/training-reminders/index.ts; absent from supabase/config.toml
Status: STILL OPEN (2 runs — prior watch item + Prompt 14 item 4)
Confidence: certain
Violates: "Register the training-reminders function in supabase/config.toml with the correct verify_jwt for its caller" — audit-2026-07-30 Prompt 14 item 4
Problem: Nothing in the repo invokes it (no client call, no cron migration); either dead code to delete or wiring that never landed.
Blast radius: deployed-function surface; 401/404 risk if an out-of-band cron exists.

### [P3-03] No tests on the bypass gate or goal visibility rules
Dimension: 7 — Location: src/hooks/useChecklistGating.ts, supabase/functions/checklist-bypass/index.ts, src/hooks/useGoals.ts — no corresponding files in src/test/
Status: NEW · Confidence: certain
Violates: P3 definition — "missing tests on rule-enforcing paths" (contrast: closeout and streak logic got tests via extracted pure libs)
Problem: The clock-out gate is the app's one hard accountability rule and its logic is untestable as written (hook + edge function); client and server already compute the gating set differently (P1-14) and nothing pins either. No test covers private-goal visibility or the (unbuilt) S+M gate.
Blast radius: gating regressions (clock-out trapping or silent bypass) ship undetected.

### [P3-04] Sprint-churn duplication: two ProgressRings, three date-shift helpers
Dimension: 7 — Location: src/components/SprintCard.tsx (local ProgressRing) vs src/components/goals/ProgressRing.tsx; shiftDate in src/lib/momentum.ts and src/pages/DepositLog.tsx; third variant family in src/hooks/useChecklists.ts (shiftAnchor/mondayOf)
Status: NEW · Confidence: certain
Problem: Same UTC-noon date math re-implemented per file; the two rings will drift independently.
Blast radius: future visual/date-logic divergence.

### [P3-05] fof-assistant edge function orphaned
Dimension: 7 — Location: supabase/functions/fof-assistant/ + supabase/config.toml:21-23; zero callers in src/ (widget uses kimi-agent)
Status: NEW · Confidence: certain
Problem: Function, config entry, and doc references survive although nothing invokes it; any live deployment is an unmaintained AI endpoint. (Its POLICY_SUMMARY content is P2-05; dedupe the fix with the kimi-agent migration.)
Blast radius: _shared/procedure-notes.ts comment drift.

### [P3-06] FOF Assistant no-PHI note only renders in the empty-chat state
Dimension: 4 — Location: src/components/fof/FofAssistantWidget.tsx:228-232
Status: NEW · Confidence: certain
Violates: the established persistent-reminder pattern — src/pages/Assistant.tsx:196-198 (REVIEW F11)
Problem: "Never include patient names…" disappears after the first message, unlike the Ask AI reminder that persists under the input.
Blast radius: minor; same verbatim-forward class as REVIEW open item 2.

### [P3-07] Stale "TimeVault" brand on MCP/agent surfaces
Dimension: 8 — Location: src/lib/mcp/index.ts ("timevault-mcp"/"TimeVault"), supabase/functions/mcp/index.ts, .lovable/mcp/manifest.json, supabase/functions/kimi-agent/index.ts (SITE prompt, X-Title), supabase/functions/assistant-auditor/index.ts (X-Title), capacitor.config.json (appId), src/components/WipeDataTool.tsx (backup filename)
Status: NEW · Confidence: certain
Violates: "Rebrand incomplete… Sweep pending." — README known issue #2 (these surfaces aren't in the README's list; inventory 1.17 marks the brand "Product/no action", so this is sweep-completeness only)
Problem: MCP clients and the AI models see the old product name; README's known-issue list understates the sweep surface.
Blast radius: cosmetic/brand; kimi-agent describes the product by its old name to the model.

### [P3-08] Repo↔live schema drift: live-only tables with no migrations; broken fresh-deploy chain
Dimension: 7 — Location: goal_events, conversations, conversation_participants, messages, security_events, user_notes, employees.team exist in src/integrations/supabase/types.ts (live) with no CREATE TABLE/policy SQL in supabase/migrations/; migrations 20260730172109 + 20260730172500 build indexes/FKs/policies on those missing objects and reference undefined public.is_conv_participant; duplicate-CREATE migration 20260724130206 conflicts with 20260723200000/20260723210000
Status: NEW · Confidence: certain (repo-level)
Violates: "The repo's migration history is the schema documentation" — README §Database conventions; runbook probe culture (migrations must apply cleanly)
Problem: A fresh `supabase db reset`/deploy fails mid-chain; the migration mirror is no longer authoritative; messaging and user_notes RLS cannot be audited from the repo (user_notes "strictly owner-only RLS" — dashboard-spec — is unverifiable, needs human check on the live DB).
Blast radius: deployability; every future audit's ability to verify RLS; Lovable/live drift compounding.

---

## Diff vs last run (2026-07-30, docs/audit-2026-07-30.md)

Note: the prior report is a prose sprint audit, not protocol-format; classification is best-effort by item.

**STILL OPEN (survived 2 runs):** S+M hard gate (P1-09) · Prompt 7 edit/delete goals (P1-10) · training-reminders registration (P3-02) · allowed_users recursion (P1-11, known pre-refactor blocker) · Training auditor as spec'd (Prompt 12) — PARTIAL: no training-auditor function / training_audit_findings / fingerprints, but a different audit shape now exists (ModuleAuditPanel, admin "Awaiting review" drafts tab, audit-diff re-review badge). Suppressed from the P1 list this run; see footer.
**REGRESSED:** manager-audience checklist RLS (P0-01) · training-roleplay (P0-08).
**FIXED / verified since last run:** deposit vitals (Prompt 13 v2) landed clean — production_cents separate from collections, sliders 0–15 snap + tap-to-type overflow, same-day edits free, later edits write audit_events via trigger (20260730080338) · meeting awareness: team_meeting calendar category + next-meeting markers verified in code · new-table RLS spot-check done (this run — produced P0-01, P0-02, P0-05).
**NEW:** everything else (12 findings).
**wontfix honored:** none on record.

## Explicitly clean (audited, no findings)

- **Private-goal visibility** — enforced in RLS (can_view_goal + SELECT policy, 20260730055651:64-72,87-89); never on team grid/meeting view/Team pages.
- **Owner-only goal updates/tasks** — RLS (owns_goal, :100-107). **goal_messages owner-only** — RLS (20260730061312:18-24).
- **checklist_bypasses column restriction** — real guard trigger incl. admins; DELETE not granted; reason update path correct (20260730062218:35-56).
- **training_attempts answers privacy** — server-enforced: own-rows RLS + admins read via training_attempt_summaries() which omits answers (20260730063416).
- **Bypass UX rules** — never blocks clock-in; punch written regardless of function result; escalation = 1 + prior-day unresolved; email_send_log before enqueue_email; maskEmail in logs; in-app + email to all owners AND managers.
- **Gating math client-side** — only daily/active/per-person items gate; shared items informational; period_key formats exact (YYYY-MM-DD / week-YYYY-MM-DD Monday / YYYY-MM / YYYY), Eastern via getToday().
- **safeProcedureLabel invariant** — holds; no overrides argument; tests intact (fof-ai.test.ts, fof-code-names.test.ts); only name-visits caller sends codes-only payload.
- **checklist_bypasses.reason** — persisted + notification + escaped email only; audit event stores boolean, never the text; never touches AI.
- **Incident reports** — inline no-PHI warning present; no AI function reads them.
- **training-builder grounding/auth** — getUser + active org_members; topic guardAiInput-checked; prompt enforces fictional scenarios; reads assistant_memories/docs/settings.
- **Motivation rules** — streaks pause (never break) on PTO/closures from verified records; no public rankings/leaderboards anywhere; pulse orb expandable, no numeric score.
- **Print root pattern** — @media print hides every body child except print root for all four roots incl. goals-print-root; Radix-portal regression (225b37f) intact, not regressed.
- **No new fake-UTC writers** in write paths; period_key formats clean; bypass checklist_date Eastern.
- **Genericization Phase 1** — 14 of 15 items DONE (org_branding, deposit settings, calendar ID setting, ALLOWED_EMAILS retired, generic checklist seeds, logo to storage, fof_settings defaults scrubbed). Partial: 2.2 (P0-06).
- **Repair-tool removal** — BulkRepairTool/TimezoneRepairTool/TimeFixModal fully gone.
- **No deferred-feature widgets** stubbed on dashboards; no new Harelick hardcodes in sprint UI code (outside tests).
- **security_events / Prompt 18** — not built, spec pending (not a finding). Messaging (Prompt 17) partially built live — see P3-08.

## Suppressed findings (over cap) — 8 P1-class, highest-impact kept above

1. Practice Pulse computes only deposit-vitals signals and renders admin-only/mid-page (spec: all roles, very top, five signal classes) — src/components/PracticePulseOrb.tsx, PracticeVitalsCard.tsx:53, Dashboard.tsx:136.
2. Messaging migration chain references tables/function no migration creates (conversations/messages/is_conv_participant) — folded into P3-08's drift finding; the P1 deploy-break aspect noted there.
3. .limit(1) org-membership derivation copied into checklist-bypass, goal-assistant, training-builder (multi-org users get arbitrary-org writes) — ingest-doc open item extended; needs multi-org account to confirm exploitability.
4. user_notes "strictly owner-only RLS" unverifiable — no migration (folded into P3-08; live check required).
5. Missing inline no-PHI warnings on new AI surfaces (SetGoalCard, PathfinderChat, GoalUpdateModal, BuildModuleDialog, SprintVerifyDialog) — folded into P0-04's fix unit.
6. Collections gauge never reads monthly_collections_target — promoted to P2-11 (reclassified).
7. Goals Report footer not pinned to page bottom (spec Prompt 8) — src/index.css .goal-sheet lacks min-height/flex, .goal-page-footer uses fixed margin-top. P1-class; suppressed on cap.
8. Goals Report missing from print-invariant snapshot tests (spec Prompt 8) — src/test/print-invariant.test.tsx covers only FOF/Deposit/Incident. P1-class; suppressed on cap.
(Training auditor Prompt 12 PARTIAL also suppressed — see STILL OPEN note.)

## Could not verify this run

- Live deployment state of training-roleplay, training-reminders, office-pulse auth gateway behavior, and any out-of-band cron schedules (dashboard-only state).
- Live RLS on conversations/messages/goal_events/user_notes/security_events (created out-of-band; no migrations — P3-08).
- sprint-verification storage bucket public/private flag (created out-of-band).
- Runtime confirmation of P1-13 (static policy analysis) and P2-10 impact (window inclusivity).
- GitHub code-search negative greps are not exhaustive; absence claims were re-verified with single-term searches/direct reads but a local clone grep is the gold standard.
