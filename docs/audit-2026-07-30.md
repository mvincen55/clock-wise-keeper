# Sprint audit — 2026-07-30 (spec vs built)

Audit of everything Lovable built during the credit sprint, checked against the
specs (`docs/goals-and-bypass-spec.md`, `docs/training-library-spec.md`, README
roadmap). Method: repo file/commit inspection. Live behavior still needs
spot-testing per item.

## Landed as spec'd (verified in code)

- **Prompt 1 (Goals)** — tables, page, team grid, meeting view, private goals.
- **Prompt 2 (bypass)** — full loop: `useGuardedClockAction`, `ChecklistBypassDialog`,
  `checklist-bypass` function (in config.toml), `BypassReasonBanner` (AppLayout nag),
  `BypassReasonDialog`, `ChecklistBypassesSection` (Team manager view).
- **Prompt 3 (redesign + AI)** — componentized goals UI, one-primary-action pattern,
  PathfinderChat (persistent thread), polish with "your words" restore, SmartChips.
- **Prompt 4 (brand)** — purple everywhere, manifest updated.
- **Prompt 6 (partial)** — GoalMonthTimeline, ProgressRing, TargetProgress/smart_target,
  GoalTrainingModules on cards, breakdown can create a training module via
  training-builder (toast confirms "added to the Training Library").
- **Training Library** — tables, /training page, training-builder (deployed, in
  config.toml), assignments.
- **Prompt 9 (partial)** — training-roleplay function exists + in config.toml;
  roleplay assessments built.
- **Dashboard redesign (partial)** — `src/components/dashboard/` exists (role-shaped
  work started; sticky notes / Practice Pulse / collections meter need live
  spot-check).
- **Bonus (not spec'd, keep):** RoleGoalIdeas + goal-examples.ts (role-based goal
  suggestions — fits "defaults are the product"); training-reminders function.

## GAPS (fix these — see Prompt 14 in this file)

1. **S+M hard gate did NOT land.** `SetGoalCard` Save enables on any non-empty
   title; target is "optional"; polish failure intentionally doesn't block. SmartChips
   display but don't gate. This was the user's explicit ask (Prompt 10 item 2).
2. **Prompt 7 (edit/delete goals) NOT built.** No edit/archive UI on MyGoalCard;
   goal_events unverified; no "Changes since last meeting".
3. **Training auditor (Prompt 12) NOT built.** No `training-auditor` function, no
   `training_audit_findings`, no Needs-review badge.

## Watch items

- `training-reminders` exists as a function dir but is NOT in `supabase/config.toml`.
  Verify what calls it (cron?) and add the right `verify_jwt` entry, or it may 401/404.
- 10 new UUID-named migrations landed today — spot-check new-table RLS in the
  dashboard (goals/training/bypass tables) before the office uses them.
- Meeting awareness (team_meeting calendar category, next-meeting plan intro):
  copy references the next meeting; calendar category unverified.
- Deposit vitals (Prompt 13 v2) was in flight during this audit — verify sliders +
  4 counters + production when it finishes.

## Prompt 14 — gap closers (send after Prompt 13 v2 finishes)

> Close three spec gaps. Full specs: docs/goals-and-bypass-spec.md (Prompts 7, 10) and docs/training-library-spec.md (Prompt 12).
>
> 1. S+M HARD GATE on goal save (SetGoalCard and any edit flow): Save stays disabled until the goal is specific AND has a measurable target — concretely: non-empty polished title AND non-empty smart_target, and when polish returns SMART flags, specific+measurable must be true. Wire the gate to the existing SmartChips state with a plain hint by the failing chip ("make it measurable — add a number or count"). No red errors, no way to save without both.
>
> 2. Edit/delete goals with accountability (spec: Prompt 7 in docs/goals-and-bypass-spec.md — implement it verbatim): edit title/description on MyGoalCard (reason required if the goal has updates), archive-with-required-reason (never hard-delete), goal_events table, "Changes since last meeting" section in Meeting view, goal-assistant draft_update mentions changes.
>
> 3. Training auditor (spec: Prompt 12 in docs/training-library-spec.md — implement verbatim): training-auditor edge function (verify_jwt=true, add to supabase/config.toml), training_audit_findings with fingerprints, Needs-review badge for owners/managers on /training, fail-open.
>
> 4. Register the training-reminders function in supabase/config.toml with the correct verify_jwt for its caller (cron/service role → true).
>
> When finished: deploy training-auditor and confirm it, training-reminders, and any other new function responds (probe per docs/runbook.md §1).
